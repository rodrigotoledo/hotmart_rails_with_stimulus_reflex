<div id="<%= dom_id post %>">
  <p>
    <strong>Title:</strong>
    <%= post.title %>
  </p>

  <p>
    <strong>Description:</strong>
    <%= post.description %>
  </p>

  <p>
    <strong>Description:</strong>
    <%= post.completed_at? ? l(post.completed_at) : '--' %>
  </p>
</div>
